# prebuild-gc
services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - DATA_DIR=/app/data
    volumes:
      - app-data:/app/data

  nginx:
    build: ./nginx
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - app

volumes:
  app-data:
